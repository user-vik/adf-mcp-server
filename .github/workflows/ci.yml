name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  verify:
    name: Lint, format, audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Syntax check
        run: node --check index.js

      # Production deps only — dev-only vulnerabilities (ESLint, Prettier)
      # don't ship and shouldn't block PRs.
      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=moderate
